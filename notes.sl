+   `npm i zustand@4.3.7`

+   for time calculation
    `npm i ms@2.1.3`
    `npm i -D @types/ms`, the `-D` means it's only available in the development environment

+   for upload,
        use `npm run build` to check for errors

+   to upload:
        `vercel`

+   if vercel not installed:
        `npm i -g vercel`